version: 2.1

executors:
  linux_builder:
    docker:
      - image: cimg/base:2022.09

jobs:
  build:
    executor: linux_builder
    steps:
      - checkout
      - run:
           name: Execute shell
           command: "bash run.sh"
workflows:
  version: 2
  build_and_test:
    jobs:
      - build

