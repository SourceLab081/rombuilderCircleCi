version: 2.1
   
jobs:
  build:
    machine:
      image: linux-cuda-11:edge
    resource_class: gpu.nvidia.small.multi
    steps:
      - checkout
      - run:
           name: Execute bash script
           command: "bash buildrom.sh"
           
workflows:
  version: 2
  build_and_test:
    jobs:
      - build