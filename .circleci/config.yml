version: 2.1
   
jobs:
  build:
    machine:
      image: android:default
    resource_class: large
    steps:
      - checkout
      - run:
           name: Execute bash script
           command: "bash buildrom.sh"
           
workflows:
  version: 2
  build_and_test:
    jobs:
      - build